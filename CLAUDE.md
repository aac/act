# CLAUDE.md

Conventions for working on this codebase live in [`AGENTS.md`](AGENTS.md) — read it before making changes. It covers the documentation-discipline rule, the versioning rationale, and the project-specific build conventions.

To *use* `act` as a task tracker (the canonical claim/work/close/push loop), install the skill with `act install-skill` and run `act help`.
